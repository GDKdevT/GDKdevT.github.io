function math (a, b, operator){
    if (operator == '+'){
        return a+b;
    } if (operator == '*'){
        return a*b;
    } if (operator == '-') {
        return a-b;
    } if (operator == '%') {
        return a%b;
    } if (operator == '/') {
        return a/b;
    } 
}